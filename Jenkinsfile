pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Exécute la commande "docker version" pour vérifier la version de Docker
                sh "docker version"
            }
        }
    }
}
